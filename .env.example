# E2E / Guardian Playwright — copy to .env (never commit .env)
BASE_URL=http://127.0.0.1:4173
E2E_EMAIL=your-user@example.com
E2E_PASSWORD=your-password

# Optional: do not start local static server (you already serve the app)
# SKIP_WEB_SERVER=1
